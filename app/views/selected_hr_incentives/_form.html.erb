<%= form_for(@selected_hr_incentive) do |f| %>
  <% if @selected_hr_incentive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selected_hr_incentive.errors.count, "error") %> prohibited this selected_hr_incentive from being saved:</h2>

      <ul>
      <% @selected_hr_incentive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.hidden_field :player_id,:value=>(params[:player_id]) %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :hr_incentive_cost %><br />
    <%= f.text_field :hr_incentive_cost %>
  </div>
  <div class="field">
    <%= f.label :hr_incentive_for_round_id %><br />
    <%= f.text_field :hr_incentive_for_round_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
