<%= form_for(@student_group_case_study) do |f| %>
  <% if @student_group_case_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_group_case_study.errors.count, "error") %> prohibited this student_group_case_study from being saved:</h2>

      <ul>
      <% @student_group_case_study.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_group_id %><br />
    <%= f.text_field :student_group_id %>
  </div>
  <div class="field">
    <%= f.label :case_study_id %><br />
    <%= f.text_field :case_study_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
