<%= form_for(@price_index) do |f| %>
  <% if @price_index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_index.errors.count, "error") %> prohibited this price_index from being saved:</h2>

      <ul>
      <% @price_index.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :round_id %><br />
    <%= f.text_field :round_id %>
  </div>
  <div class="field">
    <%= f.label :brand_id %><br />
    <%= f.text_field :brand_id %>
  </div>
  <div class="field">
    <%= f.label :price_index %><br />
    <%= f.text_field :price_index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
