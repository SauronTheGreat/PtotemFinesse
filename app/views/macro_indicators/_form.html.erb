<%= form_for(@macro_indicator) do |f| %>
  <% if @macro_indicator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@macro_indicator.errors.count, "error") %> prohibited this macro_indicator from being saved:</h2>

      <ul>
      <% @macro_indicator.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
