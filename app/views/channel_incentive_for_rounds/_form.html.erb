<%= form_for(@channel_incentive_for_round) do |f| %>
  <% if @channel_incentive_for_round.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@channel_incentive_for_round.errors.count, "error") %> prohibited this channel_incentive_for_round from being saved:</h2>

      <ul>
      <% @channel_incentive_for_round.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :channel_incentive_id %><br />
    <%= f.text_field :channel_incentive_id %>
  </div>
  <div class="field">
    <%= f.label :round_id %><br />
    <%= f.text_field :round_id %>
  </div>

  <div class="field">
    <%= f.label :min_limit %><br />
    <%= f.text_field :min_limit %>
  </div>

  <div class="field">
    <%= f.label :max_limit %><br />
    <%= f.text_field :max_limit %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
