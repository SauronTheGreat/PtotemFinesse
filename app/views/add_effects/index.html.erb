<h1>Listing add_effects</h1>

<table>
  <tr>
    <th>Media</th>
    <th>Ratio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @add_effects.each do |add_effect| %>
  <tr>
    <td><%= add_effect.media_id %></td>
    <td><%= add_effect.ratio %></td>
    <td><%= link_to 'Show', add_effect %></td>
    <td><%= link_to 'Edit', edit_add_effect_path(add_effect) %></td>
    <td><%= link_to 'Destroy', add_effect, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Add effect', new_add_effect_path %>
