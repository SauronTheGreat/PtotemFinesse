<%= form_for(@ad_index) do |f| %>
  <% if @ad_index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_index.errors.count, "error") %> prohibited this ad_index from being saved:</h2>

      <ul>
      <% @ad_index.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :round_id %><br />
    <%= f.text_field :round_id %>
  </div>
  <div class="field">
    <%= f.label :brand_id %><br />
    <%= f.text_field :brand_id %>
  </div>
  <div class="field">
    <%= f.label :ad_index %><br />
    <%= f.text_field :ad_index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
