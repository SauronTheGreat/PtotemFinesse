<h1>Listing employee_expenses</h1>

<table>
  <tr>
    <th>Round</th>
    <th>Player</th>
    <th>Employee efficiency</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employee_expenses.each do |employee_expense| %>
  <tr>
    <td><%= employee_expense.round_id %></td>
    <td><%= employee_expense.player_id %></td>
    <td><%= employee_expense.employee_efficiency %></td>
    <td><%= link_to 'Show', employee_expense %></td>
    <td><%= link_to 'Edit', edit_employee_expense_path(employee_expense) %></td>
    <td><%= link_to 'Destroy', employee_expense, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee expense', new_employee_expense_path %>
