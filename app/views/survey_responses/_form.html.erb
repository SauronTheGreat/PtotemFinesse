<%= form_for(@survey_response) do |f| %>
  <% if @survey_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_response.errors.count, "error") %> prohibited this survey_response from being saved:</h2>

      <ul>
      <% @survey_response.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.text_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :student_group_survey_id %><br />
    <%= f.text_field :student_group_survey_id %>
  </div>
  <div class="field">
    <%= f.label :question_option_id %><br />
    <%= f.text_field :question_option_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
