<%= form_for(@consumer_type) do |f| %>
  <% if @consumer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consumer_type.errors.count, "error") %> prohibited this consumer_type from being saved:</h2>

      <ul>
      <% @consumer_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :purchase_quantity %><br />
    <%= f.text_field :purchase_quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
