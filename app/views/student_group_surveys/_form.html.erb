<%= form_for(@student_group_survey) do |f| %>
  <% if @student_group_survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_group_survey.errors.count, "error") %> prohibited this student_group_survey from being saved:</h2>

      <ul>
      <% @student_group_survey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_group_id %><br />
    <%= f.text_field :student_group_id %>
  </div>
  <div class="field">
    <%= f.label :survey_id %><br />
    <%= f.text_field :survey_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
