<%= form_for(@channel_incentive) do |f| %>
  <% if @channel_incentive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@channel_incentive.errors.count, "error") %> prohibited this channel_incentive from being saved:</h2>

      <ul>
      <% @channel_incentive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :incentive_type %><br />
    <%= f.check_box :incentive_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
